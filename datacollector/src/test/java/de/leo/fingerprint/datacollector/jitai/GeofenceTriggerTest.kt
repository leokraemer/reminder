package de.leo.fingerprint.datacollector.jitai

/**
 * Created by Leo on 11.01.2018.
 */
class GeofenceTriggerTest {
//TODO integretion test, due to use of android.location.Location
   /*var geofence1: MyGeofence = MyGeofence(LatLng(1.0, 45.0), 50f)
    var geofence2: MyGeofence = MyGeofence(LatLng(2.0, 45.0), 50f)
    var geofence3: MyGeofence = MyGeofence(LatLng(3.0, 45.0), 50f)
    val sensorData = SensorDataSet(System.currentTimeMillis(), "dummy")
    @Before
    fun setup() {
        val location = Location("")
        location.latitude = 1.0
        location.longitude = 45.0

        sensorData.gps = location
    }

    @Test
    fun singeGeofence() {
        val trigger = GeofenceTrigger(listOf(geofence1))
        Assert.assertTrue(trigger.check(sensorData))
    }*/

}